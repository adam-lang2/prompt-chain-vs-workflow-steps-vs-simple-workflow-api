# See README.md "Setup" -- every agent and the eval judge/simulator call
# DeepSeek via OpenRouter, authenticated with this one key.
OPENROUTER_API_KEY=

# TypeSafe API key for the jev agent (TypeSafe's Jev model).
TYPESAFE_API_KEY=
